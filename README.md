# Тренажёр устной части ЕГЭ по китайскому языку

Локальный тренажёр по демоверсиям ЕГЭ 2021–2026 и открытому варианту 2026 года. В нём есть полный экзаменационный режим, отдельная тренировка заданий 1–3, официальные тайминги каждого года, автоматические переходы, запись с микрофона, история прогресса и аккаунты с синхронизацией через SQLite.

## Запуск

Из папки проекта выполните:

```bash
python3 server.py
```

Затем откройте `http://127.0.0.1:8080`. Для записи разрешите браузеру использовать микрофон.

Переключатель «Быстрый режим» сокращает таймеры и удобен для демонстрации. Записи хранятся только в памяти текущей вкладки и исчезают после её закрытия.

## Прогресс и аккаунты

- Без входа история и настройки сохраняются в `localStorage` текущего браузера.
- После регистрации или входа прогресс синхронизируется с SQLite-базой `var/trainer.sqlite3`.
- Пароли хранятся только в виде PBKDF2-хешей с индивидуальной солью.
- Сессия передаётся в `HttpOnly` cookie со сроком 30 дней.

Папка `var/` исключена из Git и не должна публиковаться. Для публичного размещения запускайте сервер за HTTPS-прокси и установите переменную `TRAINER_SECURE_COOKIE=1`.

## Добавление вариантов

Каждый вариант хранится отдельно в `data/variants/`. Чтобы добавить новый, создайте JSON по структуре существующих файлов и зарегистрируйте его в `data/variants/index.json`. Изображения вариантов находятся в `assets/variants/<год>/`.
